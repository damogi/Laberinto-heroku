#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../generate-maze-by-clustering/bin/generate-maze-by-clustering" "$@"
  ret=$?
else 
  node  "$basedir/../generate-maze-by-clustering/bin/generate-maze-by-clustering" "$@"
  ret=$?
fi
exit $ret
